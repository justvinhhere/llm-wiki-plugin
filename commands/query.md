---
description: Ask a question against the wiki — answer with citations, auto-file synthesis
argument-hint: <question>
---

Use the `wiki-query` skill to answer `$ARGUMENTS` using only the current wiki's content. Classify the question as lookup or synthesis; for synthesis, file the answer as a query page and gate the commit on the user's diff review.
