---
description: Trace a wiki claim back to the ingest and raw source that introduced it
argument-hint: <page> [line-range-or-phrase]
---

Use the `wiki-blame` skill on `$ARGUMENTS`. Surface the SHA, ingest scope, source slug, and the corresponding passage in the raw file.
