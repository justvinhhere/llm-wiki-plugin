---
description: Narrate the evolution of a single wiki page
argument-hint: <page> [--diffs]
---

Use the `wiki-history` skill to produce a narrated timeline of the commits that touched `$ARGUMENTS`. If `--diffs` is passed, include each commit's diff inline.
