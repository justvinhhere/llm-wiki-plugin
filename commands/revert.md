---
description: Undo a wiki mutation commit via git revert
argument-hint: [<sha>]
---

Use the `wiki-revert` skill. If `$ARGUMENTS` is empty, list recent wiki-mutation commits and let the user pick. Otherwise revert the given SHA.
