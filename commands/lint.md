---
description: Structural health check of the wiki — orphans, dead links, stale pages
argument-hint: [--since <git-ref>]
---

Use the `wiki-lint` skill to audit the current wiki. Produce a report only — do not auto-fix findings. If `--since <ref>` is passed, scope the scan to files changed since that ref.
