---
description: Ingest a source into the wiki
argument-hint: <path-or-url>
---

Use the `wiki-ingest` skill to read and file the source at `$ARGUMENTS` into the current wiki. If no argument is given, ask the user for the source path or URL.
